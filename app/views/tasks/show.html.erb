<h1>This is my Task: <%= @task.title %></h1>
<p>Details: <%= @task.details %></p>
<%= form_for @task do |f| %>
  <div class="field">
    <%= f.label :completed %>
    <%= f.check_box :completed %>
  </div>
  <%= f.submit "Complete Task" %>
<% end %>
<%= link_to "Back", tasks_path %>
